export const mockSummary = {
  text: 'Closures in JavaScript happen when a function retains access to variables from its outer lexical scope even after the outer function has finished executing.',
  bulletPoints: [
    'Closures preserve lexical scope.',
    'They are common in callbacks, event handlers, and factory functions.',
    'They help model private state and encapsulation.',
  ],
}